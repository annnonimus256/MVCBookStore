@model BookStore.Models.BookModel

@{
    ViewBag.Title = "Book";
}



<h2>@Model.Name</h2>

<tr>
    <td><p><b>Author: </b>@Model.Autor</p></td>
    <td><p><b>Price: </b>@Model.Price</p></td>
    <td><p><b>Seller: </b>@Model.UserName</p></td>
</tr>


@*<div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="зарегестрироваться" class="btn btn-default" />
        </div>
    </div>*@


@if (Model.UserId.ToString() != User.Identity.Name)
{
    <button class="btn btn-default"> <p>@Html.ActionLink("Купить", "BuyBook", new { id = Model.Id })</p></button>

}
@if (Model.UserId.ToString() == User.Identity.Name)
{
    <button class="btn btn-default"><p>@Html.ActionLink("Удалить", "DeleteBook", new { id = Model.Id })</p></button>
}

<div style="margin-top:20px">
    @Html.ActionLink("Back to List", "Buy", "Home")
</div>



